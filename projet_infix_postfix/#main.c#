#include <stdio.h>
// (4 + 3) * 2

// 4 3 + 2 *


int taille(char str[])
{
  int i = 0;
  while (str[i] != '\0')
    {
      i = i + 1
    }
  return i;
}
void lire_entre(char tableaux[]) {
  char curr_char;
  while scanf("%c", &curr_char)
}

int main(int argc, char *argv[])
{
  
}